[phases.setup]
nixPkgs = ["python311", "nodejs_20"]

[phases.install]
cmds = [
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "python3 -m uvicorn reanime:app --host 0.0.0.0 --port $PORT"
